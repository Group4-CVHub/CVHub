@model CVHub.Models.User

    <link rel="stylesheet" href="~/css/myPage/MyPage.css" />
    <link rel="images" href="~/css/myPage/images" />
    <script src="~/js/MyPage/MyPage.js"></script>

    <div class="container rounded shadow p-3 bg-white mt-5 mb-5">
        <div class="row">
            <div class="col-md-3 border-right">
                <div class=" d-flex flex-column align-items-center text-center">
                    <img class="rounded-circle mt-5" src="/css/myPage/images/avatar.png" id="photo" width="90">
                    <span class="font-weight-bold">@Model.FirstName @Model.LastName</span>
                    <span class="text-black-50">@Model.Email</span>
                    <span>@Model.Country</span>
                </div>
                <div class="mt-5 text-center"><a class="btn btn-outline-primary profile-button" asp-controller="Cv" asp-action="CvList" type="button">My CVs</a></div>
                <div class="mt-2 text-center"><a class="btn btn-outline-primary profile-button btn-sm" asp-action="Update" asp-route-Id="@Model.UserId" type="button">Edit Profile</a></div>
            </div>
            <div class="col-md-6">
                <div class="p-3 py-5">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h6 class="text-right">My Page</h6>
                    </div>
                    <div class="row mt-3">
                        <div class="profile-form col-md-6">
                            <label class="labels">First name</label>
                            <br />
                            <span id="firstName"> @Model.FirstName </span>
                        </div>
                        <div class="profile-form col-md-6">
                            <label class="labels">Surname</label>
                            <br />
                            <span id="lastName">@Model.LastName</span> 
                        </div>
                        <div class="profile-form col-md-12">
                            <label class="labels">Email</label>
                            <br />
                            <span id="email">@Model.Email</span>
                        </div>
                        <div class="profile-form col-md-12">
                            <label class="labels">Phone Number</label>
                            <br />
                            <span id="phonenumber">@Model.PhoneNumber</span>
                        </div>
                        <div class="profile-form col-md-6">
                            <label class="labels">City</label>
                            <br />
                            <span id="city">@Model.City</span>
                        </div>
                        <div class="profile-form col-md-6">
                            <label class="labels">State/Region</label>
                            <br />
                            <span id="state">@Model.State</span>
                        </div>
                        <div class="profile-form col-md-12">
                            <label class="labels">Country</label>
                            <br />
                            <span id="country">@Model.Country</span>
                        </div>
                        <div class="profile-form col-md-12">
                            <label class="labels">LinkedIn</label>
                            <br />
                            <span id="country">@Model.LinkedIn</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
